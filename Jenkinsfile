pipeline {
    agent any

    stages {
        stage('One') {
            steps {
                echo "stage one"
            }
        }

        stage('One') {
            steps {
                echo "stage one"
            }
        }

        stage('One') {
            steps {
                echo "stage one"
            }
        }
    }
}